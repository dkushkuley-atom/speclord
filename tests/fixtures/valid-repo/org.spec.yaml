specVersion: "0.1.0"
organization: "test-org"

stack:
  runtime: "python"
  language: "python"

patterns:
  naming: "snake_case"
  testing: "pytest"

security:
  - "Validate all input"
  - "Never log credentials"
