specVersion: "0.1.0"
organization: "speclord"

stack:
  runtime: "python"
  language: "python"

patterns:
  naming: "snake_case"
  testing: "pytest"

security:
  - "Validate all input"
  - "Raise typed errors, never bare Exception"
